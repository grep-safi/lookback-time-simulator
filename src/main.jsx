import React from 'react';
import ReactDOM from 'react-dom';
import LookbackTimeSimulator from './Lookback-Time-Simulator.jsx';

const domContainer = document.querySelector('#sim-root');
ReactDOM.render(<LookbackTimeSimulator />, domContainer);
